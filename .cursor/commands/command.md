# command

Commande 1 — /brand_p0_fix

Corrige tout ce qui est bloquant (edit/duplicate/realtime/RPC mismatch/messages authz).

Commande 2 — /brand_ui_kit

Crée/itère sur le design system brand-ui (tokens + components + variants).

Commande 3 — /brand_refactor_dashboard

Refond dashboard selon Brand UI + number-flow + realtime + states premium.

Commande 4 — /brand_refactor_contests_list

Table finance-grade + filtres + actions + drawer mobile (vaul).

Commande 5 — /brand_refactor_contest_detail

Tabs Overview/UGC/Leaderboard/Analytics/Settings + perf.

Commande 6 — /brand_upload_real

Remplace upload simulé par Supabase Storage + progress UI.

Commande 7 — /brand_messages_deeplink

Deep-link depuis modération → messages avec thread sélectionné / création autorisée.

Commande 8 — /brand_release_check

Checklist finale avant merge :

a11y/perf/security,

tests,

no TODO placeholders,

bundle check.

This command will be available in chat with /command
