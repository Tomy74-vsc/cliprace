import type { ReactNode } from 'react';

/**
 * Contest Detail Layout
 * Reuses the main brand layout chrome (BrandShell, nav, top bar) from /app/brand/layout.
 * This file mainly exists to keep the route structure explicit for the contest hub.
 */
export default function ContestDetailLayout({ children }: { children: ReactNode }) {
  return children;
}

