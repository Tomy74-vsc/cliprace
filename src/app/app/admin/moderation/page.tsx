/*
Source:
- Tables: submissions(pending), moderation_rules, moderation_history
- Routes: /api/submissions/[id]/moderate
*/

export default function AdminModerationPage() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-8">
      <h1 className="display-2">Modération (Admin)</h1>
      <p>TODO: Toutes les submissions en attente, filtres, actions inline.</p>
    </main>
  );
}

